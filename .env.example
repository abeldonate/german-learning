# Copy this file to .env and fill in your own key. .env is git-ignored, so it
# never gets committed or pushed to GitHub.

# Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional overrides (defaults live in config.yml):
# GEMINI_MODEL=gemini-flash-lite-latest
# PORT=5000
